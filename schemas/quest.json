{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "quest",
  "title": "Quest",
  "type": "object",
  "required": ["id", "world_id", "name", "description", "objectives", "rewards"],
  "additionalProperties": false,
  "properties": {
    "id":          { "type": "string", "minLength": 1 },
    "world_id":    { "type": "string", "minLength": 1 },
    "name":        { "type": "string", "minLength": 1 },
    "description": { "type": "string" },
    "giver_npc_id": { "type": "string", "minLength": 1 },
    "region_id":   { "type": "string", "minLength": 1 },
    "level_requirement": { "type": "integer", "minimum": 1, "maximum": 20 },
    "prerequisites": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "quests_completed":    { "type": "array", "items": { "type": "string", "minLength": 1 }, "uniqueItems": true },
        "faction_standing":    { "type": "object", "additionalProperties": { "type": "integer", "minimum": -100, "maximum": 100 } },
        "flags":               { "type": "array", "items": { "type": "string", "minLength": 1 }, "uniqueItems": true }
      }
    },
    "objectives": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "description"],
        "additionalProperties": false,
        "properties": {
          "id":          { "type": "string", "minLength": 1 },
          "description": { "type": "string", "minLength": 1 },
          "optional":    { "type": "boolean", "default": false }
        }
      }
    },
    "rewards": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "currency":           { "type": "integer", "minimum": 0 },
        "items":              { "type": "array", "items": { "type": "string", "minLength": 1 }, "uniqueItems": true },
        "faction_standing":   { "type": "object", "additionalProperties": { "type": "integer", "minimum": -100, "maximum": 100 } },
        "experience_flag":    { "type": "boolean" },
        "unlocks_recipe":     { "type": "string", "minLength": 1 }
      }
    },
    "flags_on_complete": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "uniqueItems": true
    }
  }
}
